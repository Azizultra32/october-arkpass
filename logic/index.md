# Armada Logic Registry

| Rule ID | Category | Views | File |
|---------|----------|-------|------|
| Medication.PrescribedStartDate.Display | Display | UI[Pt], UI[Pr], DB | *(see `MEDICATIONS_DATE_FIELD_LOGIC.md` – pending `.all` conversion)* |
| Medication.PrescribedStartDate.AutoUpdate | Trigger | DB | *(see `MEDICATIONS_DATE_FIELD_LOGIC.md` – pending `.all` conversion)* |
| Medication.PrescribedStartDate.Validation | Validation | DB | *(see `MEDICATIONS_DATE_FIELD_LOGIC.md` – pending `.all` conversion)* |
| Medication.MultiCondition.Display | Display | UI[Pt], UI[Pr], DB | `logic/rules/display/medication-multi-condition.all` |

> Notes  
> - Rows marked “pending `.all` conversion” should be ported into formal ALL files next.  
> - Keep this table alphabetised by Rule ID for quick lookup.
